spring:
  messages:
    basename: messages/messages-error
    encoding: UTF-8
    cache-duration: 3600

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/coderumi?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: wkwjsrj895

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
